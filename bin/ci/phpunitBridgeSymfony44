#!/usr/bin/env sh

set -eu

readonly rootDir=$(realpath $(dirname $(realpath $0))/../..)

readonly SYMFONY_VERSION="4.4.*"
readonly SYMFONY_VERSION_SHORT="44"
. ${rootDir}/bin/ci/phpunitBridgeSymfony.inc.sh
